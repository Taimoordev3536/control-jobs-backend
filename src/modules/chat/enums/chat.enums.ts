export enum ConversationKind {
  Direct = 'DIRECT',
  Group = 'GROUP',
}

export enum ParticipantType {
  Admin = 'ADMIN',
  Partner = 'PARTNER',
  Employer = 'EMPLOYER',
  Client = 'CLIENT',
  Worker = 'WORKER',
}

export enum AttachmentKind {
  Image = 'IMAGE',
  Pdf = 'PDF',
}
